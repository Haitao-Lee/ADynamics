# ADynamics Dependencies
# Conditional Flow Matching for AD MRI Disease Progression Modeling
# Verified for RTX 3090 (24 GB) / CUDA 12.1 / Python 3.11
# Tested PyTorch: 2.5.1
#
# To reproduce a pinned environment from a clean install, prefer the
# `install_env.ps1` script (Windows) or the `environment.yml` (Conda).

# Core Deep Learning Framework
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# Medical Imaging
monai==1.4.0
nibabel>=5.0.0,<6.0.0
SimpleITK>=2.2.0

# Math & ODE
torchdiffeq>=0.2.5

# ANTsPy (used by utils/preprocessing/denoise.py and n4_bias_correction.py)
# Install with: pip install antspy
# The CLI tools (FSL-FAST, hd-bet) are external — see install_env.ps1 for setup.

# Scientific Computing
numpy>=1.24.0,<2.0
scipy>=1.15.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Configuration
pyyaml>=6.0

# Visualization
matplotlib>=3.7.0
pillow>=10.0.0

# Progress Bars
tqdm>=4.65.0

# Training Logging
tensorboard>=2.14.0

# Tensor Operations
einops>=0.8.0

# Development (optional)
# pytest>=7.4.0
# black>=23.0.0
# ruff>=0.1.0
