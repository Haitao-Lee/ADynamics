# ADynamics Conda Environment Specification
# Auto-generated on 2026-04-22
# For RTX 3090 / NVIDIA GPU with CUDA 12.1

name: ADynamics
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch>=2.0.0
  - torchvision>=0.15.0
  - torchaudio>=2.0.0
  - pytorch-cuda=12.1
  - pip>=26.0
  - pip:
    # Medical Imaging
    - monai==1.4.0
    - nibabel>=5.0.0,<6.0.0
    - simpleitk>=2.2.0

    # Math & ODE
    - torchdiffeq>=0.2.5

    # Data Science
    - numpy>=1.24.0,<2.0
    - scipy>=1.10.0
    - pandas>=2.0.0
    - scikit-learn>=1.3.0

    # Configuration & Utilities
    - pyyaml>=6.0
    - matplotlib>=3.7.0
    - tqdm>=4.65.0
    - einops>=0.8.0

variables:
  CUDA_VISIBLE_DEVICES: "0,1"  # Use both RTX 3090 GPUs

exports:
  PATH: "${CONDA_PREFIX}/Scripts:${PATH}"
  LD_LIBRARY_PATH: "${CONDA_PREFIX}/lib:${LD_LIBRARY_PATH}"
